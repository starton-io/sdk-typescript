/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";
import { Expose, Transform } from "class-transformer";

export class Faucet extends SpeakeasyBase {
    /**
     * Claim date.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "createdAt" })
    @Transform(({ value }) => new Date(value), { toClassOnly: true })
    createdAt?: Date;

    /**
     * Faucet claim id.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "id" })
    id: string;

    /**
     * Claim ip.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "ip" })
    ip: string;

    /**
     * Testnet network claim.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "network" })
    network: string;

    /**
     * Hash transaction of native faucet transfer.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "txHash" })
    txHash?: string;

    /**
     * UserId claim.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "userId" })
    userId?: string;

    /**
     * Claim wallet
     */
    @SpeakeasyMetadata()
    @Expose({ name: "wallet" })
    wallet: string;
}
